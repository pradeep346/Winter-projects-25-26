K = 1;       
tau = 0.5; 
G = tf(K, [tau 1]);

figure(1);
step(G);
title('Open-Loop Motor Step Response');
xlabel('Time (seconds)');
ylabel('Amplitude (Speed/Throttle)');
grid on;

if ~exist('results', 'dir')
    mkdir('results');
end
saveas(gcf, 'results/open_loop_response.png');

disp('Open-Loop Plant Characteristics:');
stepinfo(G)